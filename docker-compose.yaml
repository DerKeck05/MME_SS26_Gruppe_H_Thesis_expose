services:
  backend:
    build: .
    ports:
      - '9000:9000'

  db:
    image: postgres:latest
    environment:
      POSTGRES_USER: group_h
      POSTGRES_PASSWORD: thesis_g_h
      POSTGRES_DB: group_h_db
    volumes:
      - postgres_data:/var/lib/postgresql

volumes:
  postgres_data: